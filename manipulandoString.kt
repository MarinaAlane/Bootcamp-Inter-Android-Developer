// Pode ser concatenada com plus/+ => um método não seguro
// tbm é tratada como um array de char

// Indexação - First(), last(), length() e index[0]

// Concatenação parecida com o template string do JS

// // função principal
fun main() {
  val name = "Marina"
    val lastName = "Alane"
  println(name + lastName)
// A melhor forma nesse cenário
println("${name} ${lastName}")
// RANGE = ..
println("Oi, ${name}")
}

// Método de formatação

// capitalize() = > primeira letra maiúscula
// decapitalize => primeira letra minúsculo

// toUpperCase e toLowerCase => como no JS


// trim() -> retira espaco vazio da string 
// trimEnd(), trimStart()

// SUbstituir caracter replace(x,y) => Como no JS